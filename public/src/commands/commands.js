/*
 * Lógica para comandos (FunctionFile).
 * Por enquanto, não há comandos específicos para o botão de barra de ferramentas.
 */

/* global Office */

Office.onReady(() => {
    // Pronto para comandos de UI se necessário futuramente.
});
